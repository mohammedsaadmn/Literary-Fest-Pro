# Port for backend server (Render automatically sets process.env.PORT in production)
PORT=5000

# MongoDB Atlas Connection String (Add your real Atlas URI in Render environment variables)
MONGO_URI=your_mongodb_atlas_connection_string

# Environment Mode
NODE_ENV=production

# Frontend Client URL for CORS policy in production
CLIENT_URL=https://your-frontend-domain.vercel.app
